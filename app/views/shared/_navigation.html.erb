<nav class="bg-white shadow-sm border-b border-gray-200 sticky top-0 z-50" data-controller="mobile-menu">
  <div class="px-6">
    <div class="flex items-center justify-between h-16">
      <!-- Logo/Brand -->
      <div class="flex items-center">
        <%= link_to root_path, class: "flex items-center space-x-2" do %>
          <span class="text-xl font-bold">Profit Path</span>
          <span class="text-xs text-gray-500">A Trade Tracking Journaling App</span>
        <% end %>
      </div>

      <!-- Desktop Navigation -->
      <div class="hidden md:flex items-center space-x-8">
        <% if user_signed_in? %>

          <!-- User Dropdown -->
          <div class="relative" data-controller="dropdown">
            <button data-action="click->dropdown#toggle"
                    class="flex items-center space-x-2 text-gray-700 hover:text-gray-900 focus:outline-none focus:ring-2 focus:ring-blue-500 rounded-md px-2 py-1 cursor-pointer">
              <span class="text-sm font-medium"><%= current_user.email.split("@").first %></span>
              <svg class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" />
              </svg>
            </button>

            <div data-dropdown-target="menu"
                 class="hidden absolute right-0 mt-2 w-48 bg-white rounded-md shadow-lg py-1 z-50 border border-gray-200">
              <%= link_to edit_user_registration_path,
                    class: "block px-4 py-2 text-sm text-gray-700 hover:bg-gray-100" do %>
                <div class="flex items-center space-x-2">
                  <svg class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z" />
                  </svg>
                  <span>Edit Profile</span>
                </div>
              <% end %>

              <div class="border-t border-gray-100"></div>

              <%= link_to destroy_user_session_path,
                    method: :delete,
                    data: {"turbo-method": :delete},
                    class: "block px-4 py-2 text-sm text-gray-700 hover:bg-gray-100" do %>
                <div class="flex items-center space-x-2">
                  <svg class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1" />
                  </svg>
                  <span>Sign Out</span>
                </div>
              <% end %>
            </div>
          </div>
        <% else %>
          <%= link_to "Sign in",
                new_user_session_path,
                class: "text-gray-700 hover:text-gray-900" %>
          <%= link_to "Sign up",
                new_user_registration_path,
                class: "bg-blue-600 hover:bg-blue-700 text-white font-semibold py-2 px-4 rounded-lg transition-colors" %>
        <% end %>
      </div>

      <!-- Mobile menu button -->
      <button class="md:hidden p-2" data-action="click->mobile-menu#toggle">
        <svg class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
        </svg>
      </button>
    </div>
  </div>

  <!-- Mobile Navigation Menu -->
  <div class="md:hidden hidden" data-mobile-menu-target="menu">
    <div class="px-2 pt-2 pb-3 space-y-1 bg-white border-t border-gray-200">
      <% if user_signed_in? %>

        <div class="px-3 py-2 text-sm text-gray-600 border-b border-gray-200">
          Signed in as <%= current_user.email.split("@").first %>
        </div>

        <%= link_to edit_user_registration_path,
              class: "block px-3 py-2 text-gray-700 hover:bg-gray-50 rounded-md" do %>
          <div class="flex items-center space-x-2">
            <svg class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z" />
            </svg>
            <span>Edit Profile</span>
          </div>
        <% end %>

        <%= link_to destroy_user_session_path,
              method: :delete,
              data: {"turbo-method": :delete},
              class: "block px-3 py-2 text-gray-700 hover:bg-gray-50 rounded-md" do %>
          <div class="flex items-center space-x-2">
            <svg class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1" />
            </svg>
            <span>Sign Out</span>
          </div>
        <% end %>
      <% else %>
        <%= link_to "Sign in",
              new_user_session_path,
              class: "block px-3 py-2 text-gray-700 hover:bg-gray-50 rounded-md" %>
        <%= link_to "Sign up",
              new_user_registration_path,
              class: "block px-3 py-2 text-gray-700 hover:bg-gray-50 rounded-md" %>
      <% end %>
    </div>
  </div>
</nav>
